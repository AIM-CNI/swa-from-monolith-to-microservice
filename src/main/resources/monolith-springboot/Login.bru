meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: http://localhost:8081/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {"username":"rody", "password":"rody"}
}

settings {
  encodeUrl: true
}
