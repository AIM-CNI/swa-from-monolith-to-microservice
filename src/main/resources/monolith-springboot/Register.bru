meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: http://localhost:8081/api/auth/register
  body: json
  auth: inherit
}

body:json {
  {"username":"rody", "password": "rody"}
}

settings {
  encodeUrl: true
}
